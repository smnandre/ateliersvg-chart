name: CI

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cs:
    uses: ateliersvg/.github/.github/workflows/CS.yml@main

  sa:
    uses: ateliersvg/.github/.github/workflows/SA.yml@main

  tests:
    strategy:
      fail-fast: false
      matrix:
        php: ['8.3', '8.4', '8.5']
    uses: ateliersvg/.github/.github/workflows/tests.yml@main
    with:
      php-version: ${{ matrix.php }}
